version: '3.8'
networks:
  sandbox: 
    name: dev-network

services:
  app:
    container_name: java-dev
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile.jdk
      args:
       - &version JAVA_VERSION=11
      labels:
       - "com.example.description=This is a development environment for Java applications."
       - "com.example.department=Development"
       - "com.example.label-with-empty-value"
       - "com.zmynxx.maintainer-email=lior.dux@develeap.com"
       - "com.zmynxx.maintainer=Lior Dux"
       - "com.zmynxx.release-date=2021-10-01"
       - "com.zmynxx.version=1.0.0"
      tags:
        # - druxx/maven-jdk:*version
        - druxx/maven-jdk:latest
    tty: true
    stdin_open: true
    working_dir: /home/developer/project
    entrypoint: ["sleep", "infinity"]
    volumes:
      - ./.m2:/home/developer/.m2
      - ./app:/home/developer/project
    # ports:
    #   - "8080:8080"
    networks:
      - sandbox 
    restart: unless-stopped
